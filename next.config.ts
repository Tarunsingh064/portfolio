/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "github-readme-stats.vercel.app",
      "leetcard.jacoblin.cool",
      'scitechindustries.com'
    ],
  },
};

module.exports = nextConfig;
